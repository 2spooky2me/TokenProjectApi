# Datasource related.
spring.datasource.url=jdbc:mysql://localhost:3306/company_tokens?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=421993

# Jpa related.
server.port=8081
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Session configuration
session.defaultTimeout=30
session.maxInactiveInterval=1800
